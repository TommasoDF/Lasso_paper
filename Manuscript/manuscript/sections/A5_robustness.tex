\section{Robustness: Feature Selection Among Significant-$\hat{\kappa}$ Stocks}\label{app:robustness_sig_kappa}

One might be concerned that the feature selection patterns documented in Section~\ref{sec:empirical} are driven by the large mass of low-volatility stocks for which the LASSO never selects any predictor.
To address this, we repeat the analysis of Section~\ref{subsec:empirical_belief_anatomy} restricting to the 155 stocks with a statistically significant $\hat{\kappa}$ estimate at the 5\% level.

The feature ranking is essentially unchanged.
The correlation between per-feature mean selection rates in the full sample and in the significant-$\hat{\kappa}$ subsample is $0.89$, and 7 of the top 10 features are identical across the two groups.
The top three features---\textit{Announce plan}, \textit{Financial reports}, and \textit{Germany}---are the same in both samples.
The Gini coefficient rises modestly from $0.18$ to $0.20$, reflecting a slight increase in concentration, but the overall distribution remains dispersed.
The macro-financial series show no systematic premium or discount relative to news topics in the restricted sample.

\paragraph{Persistence.}
We repeat the persistence analysis of Section~\ref{subsec:empirical_belief_anatomy} on the same 155 significant-$\hat{\kappa}$ stocks.
The results are nearly identical to the full sample.
Pooled across 69{,}760 selection episodes, the median run length is 4 windows and the 95th percentile is 78 windows, with a maximum of 481.
The survival curve declines from 73\% after one window to 18\% after 20 windows.
The first-order autocorrelation of the binary selection indicator, averaged across 9{,}708 active stock--feature pairs, is 0.89, decaying to 0.72 at lag 5 and 0.46 at lag 20.
Figure~\ref{fig:persistence_sig_kappa} plots the run-length distribution and survival curve for this subsample.

\begin{figure}[h]
    \centering
    \includegraphics[width=\textwidth]{persistence_sig_kappa.png}
    \caption{Persistence of Topic Selection Episodes: Significant-$\hat{\kappa}$ Stocks}
    \label{fig:persistence_sig_kappa}
    \begin{minipage}{\textwidth}
    \footnotesize
    \textit{Note:} Same as Figure~\ref{fig:topic_selection_persistence} but restricted to the 155 stocks with a statistically significant $\hat{\kappa}$ estimate at the 5\% level.
    \textbf{Left panel}: run-length distribution truncated at 80 windows; dashed line marks the median ($=4$).
    \textbf{Right panel}: survival curve $P(\text{selected at }t+k \mid \text{selected at }t)$.
    \end{minipage}
\end{figure}

\subsection{Multiple-Testing Adjustments for Second-Stage Significance}\label{app:multiple_testing}

Because the second-stage ALM is estimated stock by stock, a natural concern is that some statistically significant $\hat{\kappa}_i$ estimates may arise from multiple testing rather than from systematic evidence in favour of the model.
To address this, we re-evaluate second-stage significance using the full set of 1{,}117 stocks for which a finite $\hat{\kappa}_i$ $t$-statistic is available in the baseline estimation output.
This is the broadest feasible comparison set for standard multiplicity corrections, because it includes every stock for which the nonlinear second-stage estimator returns a usable test statistic.

\begin{table}[H]
\centering
\caption{Multiple-Testing Corrections for Second-Stage $\hat{\kappa}_i$ Significance}
\label{tab:multiple_testing}
\begin{tabular}{lcc}
\toprule
Correction method & Rejections & Share of 1,117 tests \\
\midrule
Unadjusted 5\% level & 111 & 9.9\% \\
Benjamini--Hochberg FDR 10\% & 69 & 6.2\% \\
Benjamini--Hochberg FDR 5\% & 55 & 4.9\% \\
Holm--Bonferroni 5\% & 38 & 3.4\% \\
\bottomrule
\end{tabular}
\medskip
\begin{minipage}{0.92\textwidth}
\footnotesize
\textit{Note:} The table reports the number of second-stage ALM estimates for which the null $\kappa_i = 0$ is rejected after applying alternative multiple-testing adjustments to the 1{,}117 stocks with a finite $\hat{\kappa}_i$ $t$-statistic in the baseline estimation output. The Benjamini--Hochberg procedure controls the false discovery rate, while Holm--Bonferroni controls the family-wise error rate.
\end{minipage}
\end{table}

Table~\ref{tab:multiple_testing} shows that the number of significant second-stage estimates falls materially once multiplicity is taken into account, but a non-trivial subset of stocks remains significant even under conservative adjustments.

We also conduct a global excess-significance test.
Under the joint null that each stock-level test rejects with probability 5\%, the expected number of rejections among 1{,}117 tests is 55.9.
The observed count of 111 is almost exactly double that benchmark.
An exact binomial test for excess significance rejects strongly, with $p = 1.21 \times 10^{-11}$.
This result indicates that the aggregate number of significant second-stage estimates is too large to be explained by independent 5\% false positives alone.

The interpretation of these corrections is therefore nuanced.
They do not support reading every stock-level rejection as equally strong evidence for the model, and they imply that the raw count of unadjusted significant $\hat{\kappa}_i$ estimates overstates the amount of stock-by-stock evidence.
At the same time, the corrected results do not collapse to zero: depending on the criterion, between 38 and 69 stock-level rejections survive, and the global excess-significance test rejects decisively.
Taken together, these robustness checks suggest that the empirical support for the CMCE mechanism is concentrated in a smaller subset of stocks than the unadjusted 5\% count implies, but that the overall evidence is not well described as a pure multiple-testing artefact.

\subsection{Placebo Tests for Second-Stage Identification}\label{app:placebo_stage2}

The multiple-testing corrections in Appendix~\ref{app:multiple_testing} treat the stock-level $t$-statistics as asymptotically calibrated tests.
As an additional diagnostic, we construct placebo distributions that preserve important features of the estimated belief and return panels while breaking different parts of the model-implied link between beliefs and returns.
In all cases, we keep the first-stage belief sequences fixed as estimated in the baseline specification and re-estimate the second-stage NLS equation stock by stock.
We use 100 placebo repetitions.
For each repetition, we count the number of stocks with $\hat\kappa_i$ $t$-statistic greater than 1.96.
Randomization $p$-values are computed as $(1+\#\{\text{placebo count}\geq \text{actual count}\})/(1+B)$, with $B=100$.

\begin{table}[H]
\centering
\caption{Aggregate Placebo Tests for Second-Stage $\hat{\kappa}_i$ Significance}
\label{tab:placebo_stage2}
\resizebox{\textwidth}{!}{
\begin{tabular}{lccccc}
\toprule
Test & Actual rejections & Placebo mean & Placebo median & Placebo 95th pct. & Rand. $p$-value \\
\midrule
Firm permutation & 111 & 100.9 & 101.0 & 114.1 & 0.149 \\
Within-date firm shuffle & 111 & 69.5 & 68.5 & 84.0 & 0.010 \\
Circular block shift & 111 & 111.4 & 112.0 & 126.0 & 0.564 \\
\bottomrule
\end{tabular}
}
\medskip
\begin{minipage}{0.96\textwidth}
\footnotesize
\textit{Note:} The actual baseline count is 111 significant $\hat\kappa_i$ estimates among 1{,}117 stocks with finite second-stage $t$-statistics.
The firm permutation keeps each stock's return series intact and each estimated belief series intact, but randomly assigns belief series to different stocks.
The within-date firm shuffle randomly permutes beliefs across stocks separately on each date, preserving the date-level cross-sectional distribution of beliefs while breaking stock identity.
The circular block shift keeps each stock's return series and belief series but circularly shifts the belief series by a random offset of at least 60 trading days.
\end{minipage}
\end{table}

\begin{table}[H]
\centering
\caption{Firm-Level Placebo-Adjusted $p$-Values}
\label{tab:placebo_stage2_firm_pvalues}
\begin{tabular}{lccccc}
\toprule
Placebo distribution & Firms & $p_i \leq 0.10$ & $p_i \leq 0.05$ & Median $p_i$ & 90th pct. $p_i$ \\
\midrule
Firm permutation & 1{,}117 & 112 & 60 & 0.484 & 0.910 \\
Within-date firm shuffle & 1{,}117 & 114 & 101 & 0.584 & 0.950 \\
Circular block shift & 1{,}117 & 113 & 76 & 0.554 & 0.950 \\
\bottomrule
\end{tabular}
\medskip
\begin{minipage}{0.92\textwidth}
\footnotesize
\textit{Note:} For each stock, $p_i$ is the fraction of placebo repetitions in which that stock's placebo $\hat\kappa_i$ $t$-statistic is at least as large as its actual $t$-statistic, using the same 100 repetitions as in Table~\ref{tab:placebo_stage2}.
The table reports how many stocks remain significant after replacing the asymptotic $t$-test with this stock-specific randomization benchmark.
\end{minipage}
\end{table}

The three placebo designs answer different questions.
The firm permutation is the cleanest test of whether the same stock's belief path is needed for the second-stage rejection count, because it preserves both belief persistence and return persistence but breaks the firm-level pairing.
Under this null, the actual count of 111 rejections is above the placebo mean of 100.9 but below the 95th percentile of 114.1, yielding a randomization $p$-value of 0.149.
Thus, the raw excess of significant $\hat\kappa_i$ estimates is not strong relative to a null that preserves the full time-series structure of beliefs and returns but reassigns beliefs across firms.

The within-date firm shuffle is more stringent about common time effects.
It preserves the cross-sectional distribution of beliefs on each trading day but destroys both firm identity and belief persistence within a stock.
This placebo produces a much lower rejection count, with a mean of 69.5 and a 95th percentile of 84.0.
The actual count is far in the upper tail of this distribution, with a randomization $p$-value of 0.010.
This result indicates that the second-stage relation is not explained solely by date-level common shocks and the unconditional cross-sectional dispersion of beliefs.

The circular block shift is the most demanding placebo with respect to within-firm persistence.
It preserves each firm's own belief process and return process but breaks their local timing by shifting beliefs by at least 60 trading days.
This placebo generates nearly the same rejection count as the actual data: the mean is 111.4 and the randomization $p$-value is 0.564.
The result suggests that low-frequency or persistent within-firm components of the belief and return series contribute materially to the second-stage significance.
Consistent with this interpretation, Table~\ref{tab:placebo_stage2_firm_pvalues} shows that 60 stocks are significant relative to their own firm-permutation placebo distribution, 101 relative to the within-date shuffle, and 76 relative to the circular-shift placebo.

Overall, these placebos qualify the interpretation of the second-stage $\hat\kappa_i$ tests.
The evidence is stronger than a pure date-level artifact, but it is weaker than the unadjusted asymptotic count suggests once we compare the estimates to placebo designs that preserve firm-level persistence.
We therefore interpret significant $\hat\kappa_i$ estimates as evidence of a belief-return relation concentrated in a subset of stocks, rather than as a uniformly calibrated stock-by-stock structural test.
